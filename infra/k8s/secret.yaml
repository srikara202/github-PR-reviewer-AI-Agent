# Placeholder manifest. Real values are NOT stored in this file.
# Apply real values at deploy time via one of:
#   - `kubectl create secret generic app-secrets --from-literal=KEY=VALUE ...`
#   - Sealed Secrets / External Secrets Operator / SOPS
#   - A separate, gitignored overlay (e.g. `secret.local.yaml`)
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
stringData:
  GITHUB_WEBHOOK_SECRET: ""
  GITHUB_APP_ID: ""
  GITHUB_APP_PRIVATE_KEY: ""
  OPENAI_API_KEY: ""
  LANGFUSE_PUBLIC_KEY: ""
  LANGFUSE_SECRET_KEY: ""
  DATABASE_URL: ""

